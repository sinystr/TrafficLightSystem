//
//  TrafficLightSystemTests.swift
//  TrafficLightSystemTests
//
//  Created by Georgi Georgiev on 29.10.24.
//

import Testing
@testable import TrafficLightSystem
import SwiftUI
import ViewInspector

@MainActor
struct LightBulbTests {
    // TODO fix test to find the Shape, so it can be expectable
    @Test("Test LightBulbView when isOn is true")
    func testLightBulbViewWhenIsOnIsTrue() async throws {
//        let color = Color.red
//        let view = LightBulbView(isOn: true, color: color)
//
//        let circle = try view.inspect().findAll(ViewType.Shape.self)[1]
//        
//        let foregroundColor = try circle.foregroundColor()
//        #expect(foregroundColor == color)
//
//        let opacity = try circle.opacity()
//        #expect(opacity == 1.0)
//
//        let overlayCircle = try circle.overlay().shape(0)
//        let strokeColor = try overlayCircle.foregroundColor()
//        #expect(strokeColor == Color.blue)
//
//        let shadowColor = try circle.shadow().color
//        #expect(shadowColor == color.opacity(0.5))

    }
}
